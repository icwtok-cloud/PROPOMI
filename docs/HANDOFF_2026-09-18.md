# Handoff 2026-09-18 (sesión crawler + R2 + Meta)

## Hecho
- Footer legal HernanV, INC. en producción; solicitud Meta enviada
- Crawler scale `ab7a88a`: geo AR 24 provincias (ML/InmoClick), más límites, cron cada 6h
- cron-job.org `propomi-crawler` → POST + X-Admin-Key → 202 OK
- R2 bucket + env vars en Render (upload código en este zip)

## Aplicar este zip
Ver APPLY.ps1 / README_APLICAR.txt

## Pendiente
- Front: UI de carga de fotos en panel agencia (API ya lista tras apply)
- México: research curl real → parsers
- Rotar ADMIN_KEY y R2 keys expuestas en chat
