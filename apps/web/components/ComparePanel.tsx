'use client';import {Property} from '../lib/types';import {X} from 'lucide-react';export default function ComparePanel({items,onClose}:{items:Property[];onClose:()=>void}){return <div className="modalback"><div className="modal wide"><div className="modalhead"><div><span className="eyebrow">Decision layer</span><h2>Comparador</h2><p className="muted">No buscamos solamente el precio más bajo. Buscamos qué opción tiene más sentido.</p></div><button className="close" onClick={onClose}><X/></button></div><div className="tablewrap"><table><thead><tr><th>Variable</th>{items.map(p=><th key={p.id}>{p.title}</th>)}</tr></thead><tbody>{[['Precio',p=>`USD ${p.price.toLocaleString('en-US')}`],['Superficie',p=>`${p.surface} m²`],['Ambientes',p=>p.rooms],['Zona',p=>p.zone],['Cochera',p=>p.parking?'Sí':'No'],['Crédito',p=>p.credit?'Sí':'No'],['Actualización',p=>p.freshness]].map(([k,f])=><tr key={String(k)}><td><b>{String(k)}</b></td>{items.map(p=><td key={p.id}>{(f as any)(p)}</td>)}</tr>)}</tbody></table></div></div></div>}
