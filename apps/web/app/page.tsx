'use client';
import {useEffect,useMemo,useState} from 'react';import {ArrowRight,Check,GitCompare,Heart,MessageSquare,ShieldCheck,Sparkles,CalendarDays,Handshake,BarChart3,ChevronRight} from 'lucide-react';import PropertyCard from '../components/PropertyCard';import OfferModal from '../components/OfferModal';import ComparePanel from '../components/ComparePanel';import {getProperties,trackEvent,saveIntent,listOffers,offerAction,counterOffer,getAgencyOpportunities,requestContact,shareContact} from '../lib/api';import {Intent,Property,Offer} from '../lib/types';
const levels=[['VIEW',1,'Exploración'],['SAVE',2,'Interés'],['COMPARE',3,'Evaluación'],['QUESTION',4,'Consulta'],['VISIT',6,'Intención'],['OFFER',8,'Decisión'],['NEGOTIATION',10,'Negociación'],['CONTACT_SHARED',10,'Contacto']];
export default function Home(){const [items,setItems]=useState<Property[]>([]);const [budget,setBudget]=useState('120000');const [zone,setZone]=useState('Palermo');const [rooms,setRooms]=useState('2');const [operation,setOperation]=useState('Venta');const [parking,setParking]=useState(false);const [credit,setCredit]=useState(false);const [saved,setSaved]=useState<string[]>([]);const [compared,setCompared]=useState<string[]>([]);const [offer,setOffer]=useState<Property|null>(null);const [detail,setDetail]=useState<Property|null>(null);const [tab,setTab]=useState<'buyer'|'agency'|'analytics'>('buyer');const [offers,setOffers]=useState<Offer[]>([]);const [toast,setToast]=useState('');const [agencyOpps,setAgencyOpps]=useState<any>(null);
useEffect(()=>{getProperties().then(setItems);listOffers().then(setOffers)},[]);useEffect(()=>{if(toast){const t=setTimeout(()=>setToast(''),3500);return()=>clearTimeout(t)}},[toast]);
const filtered=useMemo(()=>items.filter(p=>(!zone||p.zone===zone)&&(!rooms||rooms==='Todos'||p.rooms===Number(rooms))&&(!operation||p.operation===operation)&&p.price<=Number(budget||Infinity)&&(!parking||p.parking)&&(!credit||p.credit)),[items,zone,rooms,operation,budget,parking,credit]);const compareItems=items.filter(p=>compared.includes(p.id));
function ev(name:any,id:string){trackEvent(name,id);if(name==='property_view'){} }
function toggleSave(p:Property){const next=saved.includes(p.id)?saved.filter(x=>x!==p.id):[...saved,p.id];setSaved(next);ev('property_save',p.id)}
function toggleCompare(p:Property){if(compared.includes(p.id))setCompared(compared.filter(x=>x!==p.id));else if(compared.length<4)setCompared([...compared,p.id]);ev('property_compare',p.id)}
function openDetail(p:Property){setDetail(p);ev('property_view',p.id)}
async function askVisit(p:Property){await saveIntent(p.id,'VISIT',6,{visit:true,budget:Number(budget),timeframe:'30-60 días'});await trackEvent('visit_request',p.id,{availability:'A coordinar'});setToast('Solicitud de visita creada. La agencia puede aceptar o proponer otro horario.')}
async function askQuestion(p:Property){await saveIntent(p.id,'QUESTION',4,{budget:Number(budget)});await trackEvent('property_question',p.id);setToast('Consulta registrada como parte de tu intención. Tus datos siguen ocultos.')}
async function contact(p:Property){const r:any=await requestContact(p.id,p.agencyId||'a1');setToast(`Solicitud de contacto enviada (${r.status}). Podés decidir después si compartís tus datos.`)}
async function refreshOffers(){setOffers(await listOffers())}
return <><nav className="nav"><div className="container navin"><a href="#inicio" className="brand">propo<span>mi</span></a><div className="navlinks"><a href="#propiedades">Propiedades</a><a href="#como-funciona">Cómo funciona</a><a href="#intencion">Intención</a><button onClick={()=>setTab('agency')}>Soy agente</button></div><div className="navcta"><button className={tab==='buyer'?'tab active':'tab'} onClick={()=>setTab('buyer')}>Comprador</button><button className={tab==='agency'?'tab active':'tab'} onClick={()=>setTab('agency')}>Agencia</button></div></div></nav>
<main id="inicio"><section className="hero"><div className="container"><div className="eyebrow">La capa de intención y decisión inmobiliaria</div><h1>Encontrá una propiedad.<br/><em>Decidí. Proponé. Avanzá.</em></h1><p>Propomi convierte el interés inmobiliario en intención comercial estructurada. Descubrí, compará, evaluá, visitá y negociá sin entregar tus datos antes de tiempo.</p><div className="search"><div className="field"><label>Ubicación</label><select value={zone} onChange={e=>setZone(e.target.value)}><option>Palermo</option><option>Villa Crespo</option><option>Caballito</option><option>Belgrano</option></select></div><div className="field"><label>Presupuesto máximo</label><input value={budget} onChange={e=>setBudget(e.target.value)} inputMode="numeric"/></div><div className="field"><label>Ambientes</label><select value={rooms} onChange={e=>setRooms(e.target.value)}><option>1</option><option>2</option><option>3</option><option>Todos</option></select></div><div className="field"><label>Operación</label><select value={operation} onChange={e=>setOperation(e.target.value)}><option>Venta</option><option>Alquiler</option></select></div><button className="searchbtn" onClick={()=>document.getElementById('propiedades')?.scrollIntoView({behavior:'smooth'})}>Buscar <ArrowRight size={17}/></button></div><div className="hero-note"><span><ShieldCheck size={15}/> Datos privados por defecto</span><span><Sparkles size={15}/> Intención progresiva</span><span><Handshake size={15}/> Negociación estructurada</span></div></div></section>
<section className="section" id="propiedades"><div className="container"><div className="sectionhead"><div><div className="eyebrow">Fase 1 · Discovery + Decision</div><h2>Propiedades que tienen sentido para vos</h2><p className="muted">{filtered.length} compatibles con tus criterios actuales.</p></div><div className="filterrow"><button className={parking?'chip active':'chip'} onClick={()=>setParking(!parking)}>Cochera</button><button className={credit?'chip active':'chip'} onClick={()=>setCredit(!credit)}>Apto crédito</button><button className="chip">Balcón</button></div></div><div className="grid">{filtered.map(p=><PropertyCard key={p.id} p={p} saved={saved.includes(p.id)} compared={compared.includes(p.id)} onSave={()=>toggleSave(p)} onCompare={()=>toggleCompare(p)} onOffer={()=>setOffer(p)} onView={()=>openDetail(p)}/>)}</div>{filtered.length===0&&<div className="empty">No encontramos propiedades con estos criterios. Ampliá presupuesto, zona o ambientes.</div>}</div></section>
<section className="section darksection"><div className="container"><div className="decision-grid"><div><div className="eyebrow">Tu capacidad de compra</div><h2>Menos ruido. Más contexto para decidir.</h2><p>Con USD {Number(budget).toLocaleString('en-US')} de presupuesto, Propomi puede separar inventario compatible y alternativas cuando existen datos suficientes.</p></div><div className="metrics"><div><b>{filtered.length}</b><span>compatibles</span></div><div><b>{filtered.filter(x=>x.zone===zone).length}</b><span>en tu zona</span></div><div><b>{filtered.filter(x=>x.price<Number(budget)).length}</b><span>por debajo del presupuesto</span></div><div><b>{saved.length}</b><span>guardadas</span></div></div></div></div></section>
<section className="section" id="intencion"><div className="container"><div className="sectionhead"><div><div className="eyebrow">Fase 2 · Intent</div><h2>Cada acción demuestra un nivel diferente de intención.</h2><p className="muted">El score es configurable y no reemplaza evidencia real. Se usa para ordenar oportunidades, no para inventar probabilidades.</p></div></div><div className="intentline">{levels.map(([name,n,label])=><div key={String(name)} className="intentstep"><div className="intentnum">{n}</div><b>{String(name)}</b><span>{String(label)}</span></div>)}</div><div className="howgrid"><div className="howcard"><h3>Tu perfil de intención</h3><div className="profile"><div><span>Propiedad</span><b>La que estés evaluando</b></div><div><span>Presupuesto</span><b>USD {Number(budget).toLocaleString('en-US')}</b></div><div><span>Capital disponible</span><b>USD 80.000</b></div><div><span>Financiación</span><b>Sí / a definir</b></div><div><span>Plazo</span><b>30–60 días</b></div><div><span>Datos personales</span><b>OCULTOS</b></div></div></div><div className="howcard"><h3>El comprador mantiene el control</h3><div className="steps">{[['Guardar','Señal de interés'],['Comparar','Evaluación'],['Consultar','Contexto'],['Visitar','Intención'],['Ofertar','Decisión'],['Negociar','Avance comercial']].map(([a,b],i)=><div className="step" key={a}><div className="num">{i+1}</div><div><strong>{a}</strong><span>{b}</span></div></div>)}</div></div></div></div></section>
<section className="section alt" id="como-funciona"><div className="container"><div className="sectionhead"><div><div className="eyebrow">Fases 3 + 4</div><h2>De la intención a la negociación.</h2></div></div><div className="flowcards"><div className="flowcard"><BarChart3/><span>03 · AGENCIAS</span><h3>Oportunidades activas</h3><p>La agencia ve contexto comercial: presupuesto, timing, intención y propiedad. No ve datos personales automáticamente.</p><ul><li>Solicitar contacto</li><li>Aceptar o proponer visita</li><li>Sugerir otra propiedad</li><li>Ver oportunidades por estado</li></ul></div><div className="flowcard"><Handshake/><span>04 · NEGOCIACIÓN</span><h3>Oferta → contraoferta → avance</h3><p>La negociación queda estructurada y auditada, en lugar de perderse en mensajes aislados.</p><ul><li>Proponer precio</li><li>Aceptar / rechazar</li><li>Contraofertar</li><li>Iniciar negociación</li></ul></div><div className="flowcard"><Sparkles/><span>05 · INTELLIGENCE</span><h3>Aprender del mercado real</h3><p>Los eventos acumulados alimentan comparables, recomendaciones, demanda y pricing intelligence.</p><ul><li>Embudo de intención</li><li>Calidad de oportunidades</li><li>Demanda por zona</li><li>Insights de pricing</li></ul></div></div></div></section>
<section className="section" id="agentes"><div className="container"><div className="sectionhead"><div><div className="eyebrow">Vista de agencia · Fase 3</div><h2>No recibas “¿sigue disponible?”. Recibí oportunidades.</h2></div><button className="primary" onClick={async()=>setAgencyOpps(await getAgencyOpportunities('a1'))}>Actualizar oportunidades</button></div><div className="agencydash"><div className="side"><div className="oppstat active"><b>{agencyOpps?.active??12}</b><span>Oportunidades activas</span></div><div className="oppstat"><b>Nuevas</b><span>Intención reciente</span></div><div className="oppstat"><b>Visitas</b><span>Solicitudes</span></div><div className="oppstat"><b>Ofertas</b><span>Propuestas de precio</span></div><div className="oppstat"><b>Negociaciones</b><span>En conversación</span></div></div><div className="oppmain"><div className="opphead"><span className="pill">NUEVA OPORTUNIDAD</span><span className="muted">Datos personales ocultos</span></div><h3>Departamento 2 ambientes · USD 125.000</h3><div className="oppgrid"><div><span>INTENCIÓN</span><b>✓ Quiere visitar<br/>✓ Evalúa hacer una oferta</b></div><div><span>CAPACIDAD</span><b>Presupuesto: USD 120.000<br/>Disponible: USD 80.000<br/>Financiación: Sí</b></div><div><span>TIEMPO</span><b>30–60 días</b></div><div><span>DECISIÓN</span><b>Con pareja</b></div></div><div className="notice">La agencia puede solicitar contacto. El comprador decide si comparte sus datos.</div><button className="primary" onClick={()=>setToast('Solicitud de contacto enviada. El comprador conserva el control del consentimiento.')}>Solicitar contacto</button></div></div></div></section>
<section className="section darksection" id="analytics"><div className="container"><div className="eyebrow">Fase 5 · Intelligence</div><h2>El activo estratégico no es la cantidad de propiedades.</h2><p>Es la cantidad y calidad de intenciones reales generadas sobre ellas.</p><div className="funnel">{['Views','Saves','Comparisons','Questions','Visits','Offers','Negotiations','Contact shared','Operations'].map((x,i)=><div key={x} style={{'--i':i} as any}><b>{x}</b><span>{Math.max(1,100-i*11)}%</span></div>)}</div></div></section></main>
{compared.length>0&&<div className="comparebar"><span><GitCompare size={16}/> {compared.length} para comparar</span><button onClick={()=>setDetail(null)}>Usá el comparador desde aquí</button><button onClick={()=>setCompared([])}>Limpiar</button></div>}{compared.length>0&&<ComparePanel items={compareItems} onClose={()=>setCompared([])}/>} {offer&&<OfferModal p={offer} onClose={()=>setOffer(null)} onDone={m=>{setOffer(null);setToast(m);refreshOffers()}}/>}{detail&&<div className="modalback"><div className="modal wide"><div className="modalhead"><div><span className="eyebrow">Detalle · {detail.freshness}</span><h2>{detail.title}</h2><p className="muted">{detail.zone}, {detail.city} · {detail.source}</p></div><button className="close" onClick={()=>setDetail(null)}>×</button></div><div className="detail"><img src={detail.image} alt={detail.title}/><div><div className="bigprice">USD {detail.price.toLocaleString('en-US')}</div><p>{detail.description}</p><div className="specs large">{detail.surface} m² · {detail.rooms} ambientes · {detail.bedrooms} dormitorios · {detail.bathrooms} baño</div><div className="tags"><span>Fuente: {detail.source}</span><span>{detail.freshness}</span></div><div className="detailactions"><button className="secondary" onClick={()=>askQuestion(detail)}><MessageSquare size={15}/> Hacer pregunta</button><button className="secondary" onClick={()=>askVisit(detail)}><CalendarDays size={15}/> Pedir visita</button><button className="primary" onClick={()=>{setDetail(null);setOffer(detail)}}>Proponer precio</button></div><div className="notice"><b>Privacidad:</b> ninguna de estas acciones comparte automáticamente tu teléfono o email.</div></div></div></div></div>}
{toast&&<div className="toast"><Check size={17}/>{toast}</div>}<footer className="footer"><div className="container"><div className="brand">propo<span>mi</span></div><p>Propomi.lat · Descubrimiento, intención, decisión y negociación inmobiliaria.</p><small>Arquitectura preparada para LATAM: moneda, país, zona, fuente y unidades desacopladas.</small></div></footer></>}
