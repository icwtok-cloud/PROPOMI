'use client';import {Heart,GitCompare,ArrowUpRight} from 'lucide-react';import {Property} from '../lib/types';
export default function PropertyCard({p,saved,compared,onSave,onCompare,onView,onOffer}:{p:Property;saved:boolean;compared:boolean;onSave:()=>void;onCompare:()=>void;onView:()=>void;onOffer:()=>void}){return <article className="property"><button className="imagebtn" onClick={onView}><img src={p.image} alt={p.title}/><span className="fresh">{p.freshness}</span></button><div className="pbody"><div className="prow"><span className="muted">{p.type} · {p.zone}</span><button className="icon" onClick={onSave} aria-label="Guardar"><Heart size={18} fill={saved?'currentColor':'none'}/></button></div><h3>{p.title}</h3><strong className="price">USD {p.price.toLocaleString('en-US')}</strong><div className="specs">{p.surface} m² · {p.rooms} amb. · {p.bedrooms} dorm. · {p.bathrooms} baño</div><p>{p.description}</p><div className="tags">{p.parking&&<span>Cochera</span>}{p.balcony&&<span>Balcón</span>}{p.credit&&<span>Crédito</span>}</div><div className="actions"><button onClick={onCompare} className={compared?'secondary active':'secondary'}><GitCompare size={15}/>{compared?'Comparada':'Comparar'}</button><button onClick={onView} className="secondary">Ver detalle <ArrowUpRight size={15}/></button><button onClick={onOffer} className="primary">Proponer precio</button></div></div></article>}
